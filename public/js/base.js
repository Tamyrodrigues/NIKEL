const numeros = [1, 2, 3]; // aq se tem um array

const produto = { // aq se tem um objeto
    nome: " caneta ",
    numeracao: 0.7,
    preco: 1.99,
}

console.log(produto["nome"]); // assim vc acessar um elemento dentro do objeto.

const usuario = {
    email: "aluno@gmail.com.br"
};

usuario.nome = "aluno";
usuario.senha = "123";

console.log(usuario.email);
console.log(usuario.nome);
console.log(usuario);